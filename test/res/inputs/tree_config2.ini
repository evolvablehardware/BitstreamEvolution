; Just tests doing 2 levels of base configs

[TOP-LEVEL PARAMETERS]
base_config = ./test/res/inputs/tree_config1.ini

; To test: override one value from base, override one value from the base's base,
; and add one new param value

[SECTION 1]
; Override this parameter from both base configs
hello = hey
; Overridden from just the final base
cookie = 5678
; Overridden from just the middle-level config
cake = 1234
; New parameter
new_param = 1